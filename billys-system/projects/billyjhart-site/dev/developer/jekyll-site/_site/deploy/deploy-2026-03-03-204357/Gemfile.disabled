source 'https://rubygems.org'

# Use the Jekyll version already installed
gem 'jekyll', '4.4.1'
gem 'webrick', '~> 1.8'

# Minimal plugins for now
group :jekyll_plugins do
  gem 'jekyll-feed', '~> 0.17'
end